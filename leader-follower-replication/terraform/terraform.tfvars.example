ssh_cidr     = "1.2.3.4/32"   # replace with: curl -s ifconfig.me && echo /32
ssh_key_name = "my-key-pair"  # name of your existing AWS key pair
instance_type = "t3.large"    # 2 vCPU 8GB — runs 20 containers comfortably
